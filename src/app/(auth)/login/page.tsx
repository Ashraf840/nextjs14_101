import React from 'react'

// NB: Route Groups; Wrap the parent-dir with parentheses
// Logically group the routes ans project files without affecting the the URL path structure. Thus we don't need to mention "/auth/..." in the URL path.
export default function Login() {
    return (
        <div>Login Page</div>
    )
}
